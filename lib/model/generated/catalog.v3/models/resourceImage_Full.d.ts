/**
 * An object containing a publicly accessible image URL, or a form post that contains an image file.
 */
export type resourceImage_Full = {
    /**
     * A public URL for a GIF, JPEG, or PNG image. Limit of 8MB per file.
     *
     */
    image_url?: string;
};
